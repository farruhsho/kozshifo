# ── KO'Z SHIFO backend dependencies ───────────────────────────────────────────
# Pinned to compatible-release ranges that are known-good as of 2026-Q1.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2.0
psycopg[binary]>=3.1,<4.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.1
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
python-multipart>=0.0.9
reportlab>=4.1,<5.0
httpx>=0.27
# Cloud object storage for staff face photos (Firebase Storage / GCS) — optional;
# code degrades to a no-op if FIREBASE_STORAGE_BUCKET is unset.
google-cloud-storage>=2.16,<4.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0
