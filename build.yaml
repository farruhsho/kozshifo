# Global codegen config: map Dart camelCase fields to snake_case JSON,
# matching the FastAPI backend's response shape.
targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
